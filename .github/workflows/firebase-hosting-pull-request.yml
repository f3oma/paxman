# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Test Build Success on PR
run-name: 🚀 ${{ github.actor }} is running PaxNet Dev Deploy
on: pull_request

jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - id: 'Checkout'
        uses: actions/checkout@v4

      - id: 'Setup'
        uses: 'actions/setup-node@v1'
        with:
          node-version: 20.x

      - name: Install pnpm nx
        run: npm install -g pnpm @nrwl/cli
      
      - name: Install Dependencies
        run: pnpm install
      
      - name: 'Generate Secrets'
        working-directory: PaxNet-Web
        run: node gen_env_files.js
        env:
          ANGULAR_ENV_VARS: '${{ secrets.ANGULAR_ENV_VARS }}'
      
      - name: 'Install All'
        run: npx nx run-many --target=build --all
      
      # - name: 'Build Shared'
      #   working-directory: shared
      #   run: pnpm run build
      
      # - name: List files in shared dist folder
      #   run: |
      #     ls -R packages/shared/dist

      # - name: Create Dist Folder in Main Project
      #   working-directory: PaxNet-Web
      #   run: |
      #     mkdir -p dist/@shared
      
      # - name: List files in working folder
      #   run: |
      #     ls .
      
      # - name: Copy Shared Dist Folder to Main Project
      #   run: |
      #     cp -r packages/shared/* PaxNet-Web/dist/@shared
      
      # - name: List files in main project dist folder (after copy)
      #   run: |
      #     ls -R PaxNet-Web/dist

      # - name: 'Install Web'
      #   working-directory: PaxNet-Web
      #   run: npm install

      # - name: 'Build Web'
      #   working-directory: PaxNet-Web
      #   run: pnpm run build
      
      - id: 'Deploy'
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_F3OMAHA }}'
          projectId: f3omaha
          entryPoint: './PaxNet-Web/'
