# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
run-name: 🚀 ${{ github.actor }} is deploying PaxNet to Prod
on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - id: 'Checkout'
        uses: actions/checkout@v4

      - id: 'Setup'
        uses: 'actions/setup-node@v4'
        with:
          node-version: 20.x
      
      - id: 'Generate'
        run: node PaxNet-Web/gen_env_files.js
        env:
          ANGULAR_ENV_VARS: '${{ secrets.ANGULAR_ENV_VARS }}'

      - id: 'Install'
        working-directory: PaxNet-Web
        run: npm install

      - id: 'Build'
        working-directory: PaxNet-Web
        run: npm run build
      
      - id: 'Deploy'
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_F3OMAHA }}'
          channelId: live
          projectId: f3omaha
